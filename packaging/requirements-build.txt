# Build-time dependencies for producing the Windows executable.
#
# Install into a clean virtual environment, then run packaging/build_windows.ps1
# (which does this for you). Pulls in the app's pinned runtime dependencies plus
# PyInstaller. Tested with Python 3.10.x.
-r ../requirements.txt
pyinstaller==6.11.1
