# Build-time dependencies for producing the Windows executable and macOS .app.
#
# Install into a clean virtual environment, then run packaging/build_windows.ps1
# or packaging/build_macos.sh (which do this for you). Pulls in the app's pinned
# runtime dependencies plus PyInstaller. Tested with Python 3.10.x.
-r ../requirements.txt
pyinstaller==6.11.1
# setuptools 71+ vendors jaraco.context, which imports this on Python < 3.12.
# Needed so the frozen app can run PyInstaller's pkg_resources runtime hook.
backports.tarfile==1.2.0
